import kotlin.math.PI
import kotlin.math.truncate

//import kotlin.math.sign
//
//val x = 0.0
//val count = 10
//val shift = 0.1
//val isRight = true
//
//
//val y =
//    generateSequence(shift) { it / 2 }
//        .take(count)
//        .map { x + if (isRight) it else -it }
//        .map { -it }
//        .zipWithNext { a, b -> sign(b - a).toInt() }
//        .all { it == -1 }
//
//println(y)

//println((PI*1).mod(PI))
println((-4.9E-324).mod(PI))

//println(isNearZero((-4.9E-324).div(PI)))
//println((PI)/PI)
println(truncate(1232.123))